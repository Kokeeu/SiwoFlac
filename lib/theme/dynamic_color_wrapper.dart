import 'package:flutter/material.dart';

/// Stub: removed in Prisma redesign (Material You is gone). The wrapper now
/// just returns the supplied [theme] unchanged. Phase 3 subagents will remove
/// all call sites.
ThemeData dynamicColorWrapper(ThemeData theme, ColorScheme? scheme) {
  return theme;
}